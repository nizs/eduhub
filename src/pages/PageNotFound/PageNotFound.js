import React from 'react';
import Error from '../../components/Error/Error';

const pageNotFound = () => {
    return (
        <>
            <Error></Error>
        </>
    );
};

export default pageNotFound;